{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability set for the main Bridge window (scaffold).",
  "windows": ["main"],
  "permissions": ["core:default", "opener:default", "drag:default"]
}
